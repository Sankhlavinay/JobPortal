<nav class="navbar navbar-expand-lg navbar-dark bg-primary p-2 gap-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">JobListing</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/companies">Companies</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/jobs?type=remote">Remote Jobs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/jobs?type=onsite">Onsite Jobs</a>
        </li>
        <% if user_signed_in? %>
          <% if current_user.job_seeker? %>
            <li class="nav-item">
              <%= link_to 'Saved Job', my_saved_jobs_user_saved_jobs_path, class: "nav-link active" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Settings', settings_path, class: "nav-link active" %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Signout', destroy_user_session_path, class: "nav-link active",
            method: :delete, data: { confirm: "Are You Sure, You want to logout?" } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Signin', new_user_session_path, class: "nav-link active" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>

  <% if user_signed_in? && current_user.employeer? %>
    <div>
      <%= link_to "Create Post", new_company_job_path, class: "btn btn-info text-nowrap" %>
    </div>
    <div>
      <%= link_to "View Jobs", company_jobs_path, class: "btn btn-info text-nowrap" %>
    </div>
    <div>
      <%= link_to "All Jobs", jobs_path, class: "btn btn-info text-nowrap" %>
    </div>
  <% else%>
    <div>
      <%= link_to "All Jobs", jobs_path, class: "btn btn-info text-nowrap" %>
    </div>
  <% end %>
</nav>